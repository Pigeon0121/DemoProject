<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "file:///C:/Users/Bronya/git/DemoProject/src/main/resources/mybatis-3-mapper.dtd">
<mapper namespace="com.sjonline.mapper.UserMapper">

    <!-- 查询用户 -->
    <select id="getUserById" resultType="com.sjonline.model.User">
        SELECT 
            id, remark, del_fg AS delFlag, 
            create_time AS createTime, update_time AS updateTime,
            create_id AS createId, update_id AS updateId
        FROM user 
        WHERE id = #{id} 
          AND del_fg = 0
    </select>

    <!-- 新增用户 -->
    <insert id="saveUser">
        INSERT INTO user (
            remark, del_fg, 
            create_time, update_time, 
            create_id, update_id
        ) 
        VALUES (
            #{remark}, #{delFlag}, 
            #{createTime}, #{updateTime}, 
            #{createId}, #{updateId}
        )
    </insert>

    <!-- 删除用户 -->
    <update id="deleteUserById">
        UPDATE user 
        SET del_fg = 1 
        WHERE id = #{id}
    </update>
    
    <select id="findAllUsers" resultType="com.sjonline.model.User">
        SELECT 
            id, remark, del_fg AS delFlag, 
            create_time AS createTime, update_time AS updateTime,
            create_id AS createId, update_id AS updateId 
        FROM user 
        WHERE del_fg = 0
    </select>

    <!-- 可选：根据ID批量删除（物理删除） -->
    <delete id="deleteUsersByIds">
        DELETE FROM user 
        WHERE id IN 
        <foreach item="id" collection="list" open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>